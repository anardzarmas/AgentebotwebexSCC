# ── LLM (elige uno) ──────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=     # Claude — producción (prioridad 1)
GROQ_API_KEY=          # Groq Llama 3.2 — pruebas gratis en nube (prioridad 2)
# Si ninguna está configurada → usa Ollama local automáticamente

# ── Integraciones ─────────────────────────────────────────────────────────────
COMPOSIO_API_KEY=      # app.composio.dev → Settings → API Keys
WEBEX_BOT_TOKEN=       # developer.webex.com → My Apps → Bot Token
